# Codex Discord Bot

# Discord
DISCORD_TOKEN=your-discord-bot-token
DISCORD_GUILD_ID=your-guild-id

# OpenAI (optional — if you're already logged in via Codex App, this is not needed)
# OPENAI_API_KEY=your-openai-api-key

# Codex defaults
CODEX_MODEL=gpt-5.3-codex
CODEX_APPROVAL_MODE=never
CODEX_SANDBOX_MODE=danger-full-access

# Verbosity: 0=quiet (final only), 1=normal (tools+reasoning), 2=detailed (inputs+outputs)
VERBOSE_LEVEL=1

# Include archived Codex sessions in sync (default: false)
SYNC_ARCHIVED=false

# Database
DATABASE_PATH=./data/codex-discord.db
